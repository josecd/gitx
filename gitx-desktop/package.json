{
  "name": "gitx-desktop",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "concurrently \"vite\" \"node server.js\"",
    "server": "node server.js",
    "build": "vite build",
    "preview": "vite preview",
    "tauri:build-sidecar": "node scripts/build-sidecar.js",
    "tauri:dev": "npm run tauri:build-sidecar && npx tauri dev",
    "tauri:build": "npm run tauri:build-sidecar && npx tauri build",
    "tauri": "tauri"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.2",
    "concurrently": "^10.0.0",
    "cors": "^2.8.6",
    "esbuild": "^0.28.0",
    "express": "^5.2.1",
    "pkg": "^5.8.1",
    "vite": "^8.0.12"
  }
}
